# 進捗メモ

## 2026-08-31

- 初期仕様書を `docs/初期仕様書.md` に確定。Phase0(初期化)〜Phase1(MVP: 標準9x9将棋 + CPU戦 + プライベートマッチ)に着手開始。
- 確認を最小限にして一気に実装を進める方針（ユーザー指示）。判断に迷った点はここに記録し、後でまとめて報告する。

### 構成方針

- npm workspaces によるモノレポ:
  - `packages/rule-engine` … 将棋ルールエンジン(純粋TSロジック、フレームワーク非依存)。ここを汎用パラメータ化しておくのが最重要
  - `packages/db` … Prismaスキーマ + クライアント
  - `apps/server` … Express + Socket.io（対局サーバー、REST API）
  - `apps/web` … Next.js（フロント）
- 理由: rule-engineをパッケージ分離しておくと、将来AIワーカーやテストからも同じロジックを再利用でき、フロント/サーバーどちらからもimportできる。

### rule-engine 実装メモ

- 内部座標は `row`(0=盤面上端=後手最奥) / `col`(0=盤面右端) に統一。駒の動きは先手基準(前進=-row)で定義し、後手は`toBoardDirection`で反転させる方式にした。汎用RuleSetでも同じテーブルを使い回せる。
- 標準駒13種（歩香桂銀金角飛王+成り駒5種）を実装。合法手生成は「疑似合法手生成→自玉が王手に晒される手を除外」という素直な方式（盤面が小さい前提なので毎回全駒の利きを再計算しても十分速い）。
- **MVPでは省略した実装**（Phase2以降で必要になったら追加）:
  - 二歩は実装済みだが、**打ち歩詰め(うちふづめ)は未実装**。打ち歩による詰みも合法として扱ってしまう。
  - **千日手(同一局面の繰り返し)判定は未実装**。`drawConditions.sennichite`フラグは型として用意済みだが未接続。
  - 持将棋(27点法)も型のみ用意で未接続。
  - 手番側の合法手が0の場合は一律「手番側の負け」としている。本来は王手されていない場合の着手不能はまず起こらないはずだが、変則ルール(盤サイズ極小・自由配置など)では起こりうるので、その際の扱い(引き分け等)は要検討。
- `captureRule.mandatoryCapture`(取る一手将棋)は合法手生成のフィルタとして実装済み。標準RuleSetでは`false`にしてあるので今は無効。
- 動作確認: 標準将棋の初期局面で先手の合法手が30通りであることを確認済み（既知の値と一致）。

### DB (packages/db)

- Prismaスキーマ: `User` / `RuleSetPreset`(RuleSetをJsonでまるごと保存) / `Game` / `GameMove`。
- ローカル開発用にMac標準の`/usr/local/mysql`(8.4)がすでに起動していたのでそれを利用。`shogi_games_dev`というDBを作成し、`packages/db/.env`に`DATABASE_URL="mysql://root@localhost:3306/shogi_games_dev"`を設定してmigrate済み（ユーザー環境固有の設定。本番/他マシンでは要変更）。
- `MatchQueueEntry`(マッチング待機列)はPhase3で作る予定だが、待機列は揮発性が高くRedis向きなのでDBモデルとしては作らない方針。

### server (apps/server)

- Express(REST) + Socket.io(対局のリアルタイム同期)構成。
- 認証は未実装。MVPでは「ゲスト対局トークン」方式にした: 対局作成時にサーバーが`playerToken`をランダム生成してレスポンスで返し、クライアントはsocket接続時にそれを提示して自分の手番(sente/gote)を証明する。Userテーブルとは現状紐付いていない（ログイン機能はまだ無いため）。将来的にログイン機能を作ったら`playerToken`をUserに紐付ける形に拡張する想定。
- CPU戦: `POST /api/games/cpu`でゲーム作成 → socket.io `join`で入室 → 人間が`move`を送るとサーバーがルールエンジンで合法性検証・適用 → CPU番になったら`chooseMove`(反復深化+αβ枝刈りのnegamax、駒得評価、時間予算1.2秒)を自動実行して応手をbroadcast。
- プライベートマッチ: `POST /api/games/private`でルームコード(6桁数字)発行 → 相手が`POST /api/games/private/join`でコード入力して参加。
- 対局中の状態はメモリ上の`Map<gameId, ServerGame>`で保持(揮発性)。指し手と結果はPrismaで`GameMove`/`Game`に永続化するので、サーバー再起動で進行中の対局自体は消えるが履歴は残る設計。**サーバー再起動でも対局を継続できるようにする(メモリ状態の復元)はMVPでは未対応、Phase2以降の課題としてメモしておく。**
- 動作確認: REST + socket.ioの実通信テスト(スクリプトでCPU対局を1手進行)で、人間の着手→サーバー検証→盤面更新→CPU自動応手まで一連の流れが動くことを確認済み。

### web (apps/web)

- `create-next-app`でNext.js 16(App Router) + TypeScript + Tailwindを初期化。自動生成された`.git`は他パッケージと分離運用したくなかったため削除済み（リポジトリ全体としてはまだgit未初期化 — 別途`git init`が必要）。
- ページ構成: `/`(ロビー: CPU戦・プライベートマッチ作成・ルームコード参加) → `/game/[gameId]`(対局画面)。
- セッション管理: 対局作成時に受け取る`playerToken`/`yourColor`を`sessionStorage`に保存し、対局ページでsocket.io接続時に提示する方式（ログイン機能なしのゲスト対局のため）。**タブを閉じる/別ブラウザで開くとセッションが引き継げない点はMVPの既知の制約。**
- 盤面UI: SVGではなくシンプルな9x9のCSS Grid + ボタンで実装（先に動くものを作る優先。将来的にはドラッグ操作や駒アニメーションが欲しくなったらCanvas/SVG化を検討）。後手の駒は`rotate-180`で見分ける。
- 成り(プロモーション)の要否確認は簡易的に`window.confirm`を使用。UIとしては最低限だが、プレースホルダー用のモーダルを作るコストをかけるよりMVPではこれで十分と判断。

### 動作確認（ブラウザ実機テスト）

- agent-browserスキルでローカルNext.js(自動的にport 3000が他サービスで使用中だったためport 3001に切替)とExpressサーバー(port 4000)を起動し、実際にCPU戦を最初の1手までブラウザ操作で確認。
  - ロビー→「CPU戦を始める」→対局ページへ遷移→歩を1マス突く操作→CPU(後手)が自動で応手し盤面が更新されることをスクリーンショットで確認済み。
  - **ハマったポイント**: サーバーのCORS許可オリジンがデフォルト`http://localhost:3000`固定だったため、Next.jsが自動フォールバックした`:3001`からのfetchが`Failed to fetch`で弾かれた。`WEB_ORIGIN`環境変数で回避。本番/共同開発時はポート固定 or `.env`で明示するのが良い。

### 未対応・今後の課題（Phase2以降 or 要検討）

- 打ち歩詰め・千日手・持将棋(27点法)の判定
- サーバー再起動時の対局状態復元（現状メモリのみ保持）
- ログイン機能・レーティング反映（User紐付け）
- 盤面の向き固定（現状常に先手視点。後手プレイヤーも同じ向きで見える）
- 本番デプロイ構成（ConoHa VPSへの配置、Redis導入、環境変数管理）は未着手

## 2026-08-31（続き）: プライベートマッチ検証 + Phase2

### プライベートマッチの2人対戦検証

- agent-browserで2つの独立したブラウザセッション(p1=作成者=先手, p2=参加者=後手)を使い、ルームコード経由での参加→双方向のリアルタイム同期を実機確認。
  - p1が対局作成→ルームコード表示→p2がコード入力して参加→p1側に「相手待ち中」表示が消え両者とも対局開始。
  - p1が着手 → p2の画面に即座に反映(手番表示・盤面とも)。逆にp2が応手 → p1にも即座に反映。両方向とも問題なし。
- 前回の懸念事項だった「プライベートマッチの2人目参加」は正常動作を確認できたので、既知の課題リストから解消。

### Phase2: ルールセットの拡充

rule-engineに新しいRuleSetプリセットを追加し、サーバー/フロントから選択できるようにした。

1. **駒落ち** (`packages/rule-engine/src/ruleSets/handicap.ts`): 香落ち・角落ち・飛車落ち・飛香落ち・二枚落ち・四枚落ち・六枚落ちを標準ルールセットから該当駒を`without()`ヘルパーで取り除く形で実装。全て標準将棋の伝統的なハンデ設定(公知のルール、著作権問題なし)。
2. **相互駒落ち**: 「先手:角落ち / 後手:飛車落ち」という先後で異なる駒を落とし合うデモプリセットを1つ追加(`buildHandicapRuleSet`が`senteRemovals`も受け取れるようにして実現)。相互駒落ちは決まった型が存在しないカスタムルールなので、まずは1例のデモとして実装。将来的には自由配置UIと組み合わせて「両陣営とも自由にハンデを組める」形に発展させるのが良さそう。
3. **取る一手将棋** (`ruleSets/toruIttate.ts`): 既存の`captureRule.mandatoryCapture`フラグをオンにするだけ。合法手生成側の実装は元から用意していたので追加コードはごく少量で済んだ（Phase1の設計判断が効いた）。
4. **盤サイズ変更(5x5, 3x3)** (`ruleSets/miniBoards.ts`): **法的配慮として、どうぶつしょうぎ等の商標登録された既存の商用ミニ将棋の盤面構成・駒配置は一切参照せず、完全にオリジナルの配置を新規設計した。** 5x5は王・飛・角・銀2枚・歩5枚の構成、3x3は王+金2枚のみの詰将棋風の超ミニ対局。動作確認で「3x3では王同士が隣接できない」という将棋の基本ルールが正しく効いていることも確認できた(ルールエンジンが盤サイズに関わらず正しく汎用動作している証拠)。
5. **自由な初期盤面** (`ruleSets/freeSetup.ts` + `apps/web/app/setup/page.tsx`): スコープを絞り、「自分の陣地(下5段)だけ自由に駒を配置できる。相手(CPU or 対戦相手)は標準配置固定」という形で実装。両陣営が自由配置し合う形は次のステップ。
   - バリデーション(`validateFreeSetup`): 盤外配置・マス重複・玉が1枚以外・標準の持ち駒プール(歩9枚等)を超える配置、をサーバー側で弾く。
   - サーバー: `POST /api/games/cpu`・`/private`が`customSetup`ボディを受け取れるように拡張。カスタムルールセットは`custom_<timestamp>_<random>`というidを都度払い出し、DBには`RuleSetPreset`として毎回新規保存(プリセットのキャッシュ対象外)。
   - フロント: `/setup`ページに駒パレット(残り枚数表示)+盤面クリックで配置するUIを実装。玉が置かれるまで対局開始ボタンは無効化。

### registryの追加

- `packages/rule-engine/src/ruleSets/registry.ts`: 全プリセットを`id -> RuleSet`のマップと、フロント向けメタデータ(`RULE_SET_METADATA`: id/name/category/description)にまとめた。
- サーバー: `GET /api/games/rule-sets`でメタデータ一覧を返し、ロビー画面の`<select>`をこれで動的生成。カテゴリごとに`<optgroup>`でグルーピング(標準/駒落ち/特殊ルール/特殊盤面)。
- `apps/server/src/db.ts`の`ensureRuleSetPreset`をRuleSet単位で汎用化。プリセット名で存在チェックしてキャッシュする方式(標準プリセット用に作った仕組みをそのまま全プリセットに拡張)。カスタム自由配置だけは`custom_`prefixで判定してキャッシュせず常に新規作成。

### 動作確認

- ルールセット一覧のロジックテスト(全プリセットでGameState生成→合法手生成が例外なく動くこと、駒落ちで狙った枚数だけ減っていること)をNode.jsスクリプトで実施、全て想定通り。
- ブラウザで実際に5x5将棋のCPU戦を1手進行(歩を突く→CPU後手が斜め取りで応手・持ち駒に反映)まで確認。
- ブラウザで自由配置(王+金2枚のみ)→CPU戦開始→盤面に反映されることを確認。

### 次にやるべきこと(更新版)

- 両陣営が自由に初期配置できるモード(現状は自分の陣地のみ)
- 相互駒落ちのUI化(現状は固定デモプリセット1つのみ)
- 打ち歩詰め・千日手・持将棋(27点法)の判定
- サーバー再起動時の対局状態復元
- ログイン機能・レーティング反映
- 将棋vs囲碁・獅子(獅子王)などの特殊駒(Phase4)
- 本番デプロイ構成

## 2026-08-31（続き2）: 盤面向き修正・ルール精度向上・Phase3

### 盤面の向き修正

- `apps/web/components/Board.tsx`: 後手視点(`state.yourColor === 'gote'`)のとき、行と列の両方を反転した順序で描画するように変更(`rowOrder`/`colOrder`)。クリックハンドラには常に実座標(row,col)を渡しているので、表示順序を変えるだけでロジック側の変更は不要だった。
- 駒の回転(`rotate-180`)も「後手の駒だから」ではなく「自分の駒でないから」という判定に変更(`cell.owner !== state.yourColor`)。これで先後どちらの視点でも「相手の駒は反転して見える／自分の駒は正立」という一貫した見た目になる。
- **検証で1つハマった点**: 標準将棋の初期配置は先後で180度回転対称なので、反転の有無を初期局面のスクリーンショット比較だけでは判別できなかった(見た目が同じに見えて「反転が効いていないのでは」と誤解しかけた)。実際には1手進めた後の非対称な局面で比較したところ、正しく反転されていることを確認できた。今後同様の見た目検証をする際は、初期局面ではなく非対称な局面で比較するよう注意。

### ルールエンジンの精度向上: 千日手・打ち歩詰め

- **千日手**: `GameState`に局面(盤面+持ち駒+手番)のハッシュ的な文字列キーを持たせ、同一局面が4回出現したら`drawConditions.sennichite`が有効なルールセットでは引き分けにするよう実装(`packages/rule-engine/src/gameState.ts`の`positionKey`)。**連続王手の千日手(本来は反則負けになるべきケース)は区別せず単純な引き分けとして扱う簡易実装**、と明記してある。
- **打ち歩詰め**: 歩を打った結果が相手の即詰みになる手を合法手から除外する処理を`moveGenerator.ts`に追加(`isUchifuzume`)。判定には相手の持ち駒(応手できるかの計算に必要)が要るため、`generateLegalMoves`の引数に`opponentHand`を追加(省略可・デフォルト空オブジェクト)。歩以外の駒(金・銀等)を打って詰ませるのは将棋のルール通り合法のままなことも確認済み。
- 上記2つの実装により、標準将棋および駒落ち・取る一手・5x5/3x3/自由配置の全プリセットで`drawConditions.sennichite: true`に変更(以前はfalseで無効化していた)。
- 呼び出し側(`serialize.ts`, `ai/simpleAi.ts`)も`opponentHand`を渡すように更新。AIのnegamax探索中も打ち歩詰めチェックが効くようになったが、探索は元々時間予算(1.2秒)で打ち切る設計なので、ノード数が多少減るだけで動作自体に問題は出ない(実機で1手あたり約1.6秒、レイテンシ増加なしを確認)。
- 動作確認: Node.jsスクリプトで(1)王同士を往復させて4回目の同一局面で引き分けになること、(2)歩を打つと詰みになる局面でその歩打ちだけが合法手から除外され、同じ局面に金や銀を打つ手は合法手に残ること、をそれぞれ検証済み。

### Phase3: カジュアルマッチ・ランダムルールオンラインマッチング

- `apps/server/src/matchQueue.ts`: インメモリの待機列(配列)でマッチングを実装。Redisは未導入(仕様書ではRedis想定だったが、MVPの規模ではインメモリで十分なため簡略化。**将来的にサーバーを複数台/プロセスにスケールする際はRedis等の共有ストアへの移行が必要**、という点はここに明記しておく)。
  - カジュアルマッチ: 同じ`ruleSetId`を希望する2人が来たら即マッチ。
  - ランダムルールマッチング: 各プレイヤーが許容カテゴリ(標準/駒落ち/特殊ルール/特殊盤面)を選択→カテゴリからルールセットid集合に変換→待機中の他プレイヤーとの積集合があればマッチし、積集合からランダムに1つルールを選ぶ。積集合が空なら待機継続(段階的な条件緩和は未実装、Phase3.1的な改善候補としてメモ)。
  - 先に並んでいた側を先手に割り当てる。
- socket.ioイベント設計: `queue:join-casual` / `queue:join-random` / `queue:leave` → マッチしたら両者に`queue:matched`(gameId/playerToken/yourColor/ruleSetId)を送信。マッチしたゲームのDB永続化(RuleSetPreset確保 + Game行作成)もここで実施。
- フロント: ロビー画面に「カジュアルマッチを探す」ボタンと「ランダムルールオンラインマッチング」のカテゴリ選択チェックボックス+ボタンを追加。マッチング中はロビー内に「マッチング中…」バナー+キャンセルボタンを表示(`lib/useMatchmaking.ts`)。マッチしたら自動的に`/game/[gameId]`へ遷移。
- 動作確認: agent-browserで独立した2セッションを使い、カジュアルマッチで両者が同じ対局にマッチし、先着順で先手/後手が割り当てられることを確認。

### 次にやるべきこと(更新版)

- ランダムルールマッチングの待機時間に応じた条件緩和(現状は積集合が空だとマッチしないまま待機し続ける)
- 両陣営が自由配置できるモードへの拡張
- 相互駒落ちのUI化
- 連続王手の千日手(反則負け)の区別
- サーバー再起動時の対局状態復元
- ログイン機能・レーティング反映
- 待機列をRedis等に移行(マルチプロセス/マルチサーバー対応)
- 将棋vs囲碁・獅子(獅子王)などの特殊駒(Phase4)
- 本番デプロイ構成

## 2026-08-31（続き3）: ランダムルール条件緩和・両陣営自由配置・連続王手千日手

### ランダムルールマッチングの条件緩和

- `apps/server/src/matchQueue.ts`: ランダムルールマッチングの待機エントリに`RELAX_TIMEOUT_MS`(既定15秒、`MATCH_RELAX_TIMEOUT_MS`環境変数でテスト時に短縮可能にした)後に自動発火するタイマーを持たせ、発火時に「受け入れ範囲を全ルールセットまで緩和」→待機列を再スキャンしてマッチを試みる、という形で実装。
- 即時マッチと緩和後マッチで通知経路がずれる(前者はjoinQueueの戻り値、後者は非同期タイマー)問題は、`node:events`の`EventEmitter`(`matchEvents`)に一本化して解決。`registerMatchSocket`はサーバー起動時に1回しか呼ばれないので、リスナー登録もその中で1回だけ行うようにした(接続ごとに登録すると重複購読になるので要注意ポイントとしてコメントを残した)。
- 動作確認: カテゴリが重ならない2人(standard希望 vs boardSize希望)をキューに入れ、`MATCH_RELAX_TIMEOUT_MS=2000`で2秒後に自動的にマッチが成立することをNode.jsスクリプトで確認。

### 両陣営が自由配置できるモード

- `/setup`ページを拡張し、先手・後手それぞれに独立した駒パレット(残り枚数も先後別々にカウント)を用意。盤面は上4段=後手陣地、下4段=先手陣地、中央1段は誰も置けない中立地帯という構成に変更(以前は先手陣地のみ5段編集可・後手は標準固定だった)。
- 各陣営に「標準配置にする」「クリア」ボタンを追加。片方だけ手動で調整し、もう片方は標準に戻す、といった使い方がしやすいようにした。
- **これにより「相互駒落ちのUI化」も実質的に解決した**: 両陣営とも自由に駒を配置・除外できるので、例えば「先手は角落ち、後手は飛車落ち」のような相互駒落ちも、標準配置にした後に該当駒を1枚クリックして消すだけで作れる。専用の相互駒落ちビルダーUIを別途作る必要はないと判断し、`docs/進捗.md`の課題リストからは外した。既存の`handicap_mutual_rook_vs_bishop`のような固定デモプリセットは「よく使う組み合わせのショートカット」として引き続き残す。
- 動作確認: 両陣営を「標準配置にする」で埋めてCPU戦を開始し、通常の標準将棋と同じ初期局面で対局が始まることをブラウザで確認。

### 連続王手の千日手(反則負け)の区別

- 以前の実装は千日手を単純な引き分けとして扱っていたが、標準ルールでは「同一局面が4回出現し、かつその間ずっと一方が王手をかけ続けていた」場合はその側の反則負けになる。これを実装した。
- `GameState`に着手ごとの「その手で相手を王手にしたか」(`checkedOpponent`)と、局面キーの履歴(`positionKeyHistory`)を持たせ、千日手成立時に直近のサイクル(前回同一局面が出た地点から今回まで)を切り出して、いずれかのプレイヤーの手が全て王手だったかを判定する`findPerpetualCheckViolator`を追加。該当すれば`{status:"foul_loss", winner, reason:"perpetual_check"}`、しなければ通常の`{status:"draw", reason:"sennichite"}`を返す。
- DB: `GameResultStatus` enumに`foul_loss`を追加してマイグレーション実行済み。サーバーの結果保存ロジック・フロントの結果表示ラベルも対応。
- 動作確認: 判定ロジック(`findPerpetualCheckViolator`)を既知の履歴データで直接検証(後手が全手王手→違反者=gote、そうでなければnull、の両方を確認)。加えて、実際のゲーム進行を伴う既存の千日手テスト(王手なしの往復)が引き続き通常の引き分けとして扱われる(誤検出しない)ことも再確認した。**実戦で本当に連続王手になる局面を1から組んでE2E検証するのは盤面設計が複雑なため今回は省略し、ロジック単体テストに留めた**、という点は正直に記録しておく。

### 次にやるべきこと(更新版)

- サーバー再起動時の対局状態復元
- ログイン機能・レーティング反映
- 待機列をRedis等に移行(マルチプロセス/マルチサーバー対応)
- 将棋vs囲碁・獅子(獅子王)などの特殊駒(Phase4)
- 本番デプロイ構成

## 2026-08-31（続き4）: サーバー再起動時の状態復元・獅子王/将棋vs囲碁・追加バリアント・重大バグ修正

### 🐛 重大バグ修正: 持ち駒が減っていなかった

- **発見の経緯**: 将棋vs囲碁バリアントの動作確認中、後手が石を1枚打っても持ち駒の残り枚数が40のまま減らないことに気づいた。
- **原因**: `GameState.applyMove()`が、駒を取った時に持ち駒を+1する処理はあったが、**打った(drop)時に持ち駒を-1する処理が実装されていなかった**。Phase1からずっと存在していたバグで、標準将棋を含む全ルールセットに影響していた。
- **実害**: 一度でも駒を1枚取って持ち駒に加われば、その駒種を実質無限に打ち続けられる状態だった(合法手生成側は`count <= 0`のときだけ除外するチェックで、countが減らないので永遠に打てる)。これまでのCPU戦・プライベートマッチのテストではたまたま同じ駒を2回以上打つ場面まで進めていなかったため気づかなかった。
- **修正**: `applyMove`内で`move.type === "drop"`の場合に`this.hands[owner][move.piece]--`を追加(`packages/rule-engine/src/gameState.ts`)。将棋vs囲碁での石打ちで40→39に正しく減ることを確認し、既存の千日手・打ち歩詰め・連続王手のテストも再実行して回帰がないことを確認済み。
- **教訓**: 今後同種の「盤面は動くが持ち駒/カウンタ系の更新が漏れる」バグを防ぐため、駒を打つ系のテストは複数回同じ駒種を打つシナリオまで含めるようにする。

### サーバー再起動時の状態復元

- Prismaの`Game`モデルに`engineGameId`(gameManager側で発行するID。クライアントのURL/セッションが参照する値)・`senteToken`・`goteToken`を追加。ローカル開発DBはテストデータのみだったため`prisma db push --accept-data-loss`で直接同期(正式なmigrationファイルは作成していない。**本番運用に持っていく際はきちんとmigrationファイルを作成し直すこと**)。
- 対局作成・参加・マッチング成立の全経路(`routes/games.ts`の3エンドポイント、`matchSocket.ts`のfinalizeMatch)で、DB保存時に`engineGameId`と発行済みトークンを書き込むように変更。
- `apps/server/src/restore.ts`を新設。サーバー起動時に`status`が`in_progress`または`waiting`のGame行をDBから取得し、紐づく`GameMove`を`moveNumber`順に`GameState.applyMove()`で再生して盤面・持ち駒・手番を復元、`gameManager.registerRestoredGame()`でin-memoryストアに登録する。
- `index.ts`は`restoreGamesFromDb()`の完了を待ってから`httpServer.listen()`するようにした(復元前にリクエストを受け付けないため)。
- 動作確認: (1)CPU戦を2手進めてサーバーを再起動→同じトークンで再接続すると盤面・手番が正しく復元されることを確認。(2)プライベートマッチを作成(相手未参加のwaiting状態)してサーバーを再起動→ルームコードで2人目が問題なく参加できることを確認。

### 獅子王将棋・将棋vs囲碁の実装

- **エンジンの汎用化**: これまで「王(kind==="king")」がハードコードされていた詰み判定を汎用化。`PieceDefinition`に`isRoyal`フラグを追加し、`Board.findKing()`と`isInCheck()`が「王」だけでなく`isRoyal`指定の駒(獅子など)も"取られたら負け"の対象として扱えるようにした(`royalKindsOf()`ヘルパー)。
- **獅子(lion)駒の実装**: 中将棋由来の獅子を追加。王と同じ8方向1マスの動きに加え、`specialMove: "lion-double-step"`フラグを持つ駒は2マス先(縦横斜め8方向)にも動ける。**本来の中将棋の獅子は中間マスの駒も含めて複数回移動・複数取りできる非常に複雑な駒だが、MVPでは「中間マスの駒を無視して2マス先まで届く」という簡略化した挙動にした**、と明記しておく。獅子王将棋(`lion_king`)ルールセットは、標準将棋の王将を全てこの獅子に置き換えたバリアント。
- **将棋vs囲碁(実験的)の実装**: 完全な囲碁ルール(陣地・アタリ・コウ等)を将棋エンジンの上に実装するのは別ゲームを1つ作るに等しく非現実的と判断し、「先手は通常の将棋一式、後手は王のみ+動かせない石(打つだけで一切移動できない駒)を大量(40枚)に持つ」という非対称バリアントとして実装した。石は`moves: []`の駒として表現し、既存の合法手生成ロジック(移動手は0件、打ち手だけ生成される)がそのまま使える設計にした。
  - この実装のために`RuleSet`に`initialHands`(対局開始時点で持たせる持ち駒)フィールドを追加。GameStateのコンストラクタがこれを見て初期持ち駒を設定する。将来「最初から特定の駒を持って始まる」変則ルールにも再利用できる汎用的な拡張にした。
  - **既知の制約**: 簡易AI(`apps/server/src/ai/simpleAi.ts`)の評価関数(`PIECE_VALUES`)に`stone`・`lion`の値を設定していないため、AIはこれらの駒を「価値0」として扱う。結果、将棋vs囲碁や獅子王将棋でのCPU戦は、AIが石を打つ・獅子を活用する判断をほとんどしない(価値を感じないため)。人間同士の対局(プライベートマッチ)なら問題なく遊べるが、CPU戦としての完成度は低い。評価関数にstone/lionの適切な値を足すのがPhase5的な改善候補。
- 動作確認: ブラウザで獅子王将棋のCPU戦(獅子の表示・CPUの応手)、将棋vs囲碁のCPU戦(後手陣地が王のみ+石x40の表示、歩を突いた後のCPU応手)をそれぞれ確認。Node.jsスクリプトで獅子の2マスジャンプ(中間の自駒を飛び越えて敵駒を取れる)が正しく動くことも個別に検証済み。

### 持将棋(27点法)の実装

- 仕様書で「持将棋や引き分け関連もルールを選択できる」としていたが未実装だった`drawConditions.jishogi27`を実装。両者の玉(または獅子等のisRoyal駒)が入玉(敵陣に到達)した状態で、駒点(飛車・角・馬・龍=5点、王・獅子・石=0点、それ以外=1点)を集計し、27点以上ある側が勝ち抜け、両者27点以上なら引き分け、という一般的な数え方を自動判定する形で実装(`GameState.checkJishogi()`)。
- 標準ルールに直接混ぜ込まず、`jishogi_27`という選択可能な別プリセットとして追加した(標準ルールの挙動を暗黙に変えないため)。
- 動作確認: 先手の玉を敵陣に入れた上で27点を超える駒構成、後手は入玉のみで0点、という局面を手動構築し、先手番の手を1つ指した時点で`jishogi_win`(先手勝ち)になることを確認。
- **正直な制約**: 本来のルールは「入玉+点数」に加えて「詰みの可能性がない」という条件もあるが、そこまでは判定しておらず、単純に入玉+点数だけで自動判定している簡易版。

### その他の追加バリアント(「まだ出ていない面白いルール」の一環)

- **成り放題将棋(forced_promotion)**: 成れる場面では必ず成る。`RuleSet.promotionRule: "forced"`を追加し、`buildMoveVariants`で不成の選択肢を消すだけの小さな変更で実現。テンポの速い攻め合いになる。
- **7x7将棋(board_7x7)**: 5x5・3x3に続く中型のオリジナル盤面。香・桂を含む7種の駒構成で、5x5よりも駒がぶつかり合うまでに余裕がある設計にした。
- **千日手判定なし将棋(no_sennichite)**: カジュアルに何度でも同一局面を繰り返せる、`drawConditions.sennichite: false`の明示的な選択肢として追加(標準ルールが`true`になった今、あえて無効化したい人向け)。

### 次にやるべきこと(更新版)

- 簡易AIの評価関数にlion/stoneなどの変則駒の価値を追加する
- ログイン機能・レーティング反映
- 待機列をRedis等に移行(マルチプロセス/マルチサーバー対応)
- 本番用のPrisma migrationファイルをきちんと作成し直す(現状はdb pushで同期しただけ)
- 本番デプロイ構成

## 2026-08-31（続き5）: 棋譜出力・外部AI接続・AI強化

### 棋譜出力(KIF形式)

- `packages/rule-engine/src/kifu.ts`を新設。指し手履歴(`MoveRecord[]`)をKIF形式(将棋の標準的な棋譜フォーマット)のテキストに変換する`generateKifuText()`を実装。
  - 筋(suji)は全角数字、段(dan)は漢数字、着手元は半角数字の括弧表記、というKIFの慣例に合わせた(例: `７六歩(77)`)。
  - 直前の着手と同じマスへの着手は「同」表記に対応(`同　銀打`など)。
  - 打ち駒は「打」、成りは駒名の後に「成」を付与。
  - 終局理由(投了・詰み・反則負け・千日手・持将棋)も末尾に自動で追記。
  - **正直な制約**: 変則ルール(特殊盤面・獅子/石などの特殊駒)にも座標表記の考え方をそのまま拡張して出力はできるが、本物の将棋ソフトのKIFパーサーは9x9標準将棋・標準駒を前提にしているため、変則ルールで出力したKIFを他の将棋ソフトに読み込ませられる保証はない。
- サーバー: `GET /api/games/:gameId/kifu`で対局(進行中でも終局後でも)のKIFテキストをダウンロードできるようにした(`Content-Disposition: attachment`)。
- フロント: 対局画面に「棋譜をダウンロード(KIF)」リンクを追加。
- 動作確認: 実際にCPU戦を数手進めてKIFを取得し、座標・駒名が正しいこと、投了時に「投了」行が追記されることを確認。「同」表記もユニットテストで確認済み。

### 外部AI(ボット)接続について

ユーザーからの質問「一般の人たちに自分の持っているAIを接続して対戦を許可することを仕様として組み込めるか」への回答:

**結論: 可能。しかも、対局の基本プロトコル自体は元々ボット接続と何ら変わらない設計だったため、実はほぼ追加実装なしで動く状態だった。** これまでのE2Eテストで使っていたNode.jsスクリプト(`socket.io-client`で`join`→`state`受信→`move`送信)は、外部AIが接続する場合と全く同じ形。カジュアルマッチ/ランダムルールマッチングのキューも人間とスクリプトを区別していないため、外部AIがキューに並んで人間とマッチする、という動作もすでに成立する。

今回追加した最小限の仕組み:
- キュー参加イベント(`queue:join-casual`/`queue:join-random`)に`isBot: true`という自己申告フラグを追加できるようにした(`apps/server/src/matchQueue.ts`)。サーバーは内容を検証しない(性善説ベース)が、マッチした人間側の画面に「🤖 対戦相手はCPU/ボットです」というバッジを表示できるようにした(`ServerGame.declaredBots` → `ClientGameState.opponentIsCpuOrBot`)。フェアプレー上の開示目的。
- `docs/BOT_API.md`を新設し、接続手順・Socket.ioイベント一覧・`Move`/`ClientGameState`の型・今後の課題(レート制限・持ち時間・ボット専用レーティングは未実装)をまとめた。
- 動作確認: 実際に別プロセスのNode.jsスクリプトを「ボット」としてカジュアルマッチのキューに参加させ、ブラウザ(人間)とマッチング → 人間側に🤖バッジが表示 → 人間が着手 → ボットが応手、という一連の流れをagent-browser + 別プロセススクリプトの組み合わせで実機確認した。

**今後の課題として明記**: レート制限・持ち時間制・ボット専用レーティングは未実装。現状は「技術的に誰でも無制限に接続できる」状態なので、一般公開する前にこのあたりの防御は追加した方が良い。

### AIの強化

- 簡易AI(`apps/server/src/ai/simpleAi.ts`)の評価関数に2つ手を入れた:
  1. **獅子・石の駒価値を追加**: `lion: 0`(王と同格。合法手フィルタで実際に取られることはないため0で正しい)、`stone: 1`(それ自体に攻撃力はないが「ただで渡さない」意識づけのため)。
  2. **機動力(モビリティ)項を追加**: 葉ノードの評価に`MOBILITY_WEIGHT(0.15) × (自分の合法手数 - 相手の合法手数)`を加算。特に将棋vs囲碁バリアントのように「駒を打つこと自体が盤面制圧の価値を持つが、駒得計算上は変化しない(持ち駒→盤上に移動しても合計価値は同じ)」ルールでは、駒得評価だけではAIが石を打つ意味を全く理解できない。機動力項を入れることで、石を打って自陣を固める・相手の動きを制限する、といった判断ができるようになった。
- 実装は葉ノード(depth===0)でのみ相手側の合法手数を追加計算する形にし、探索の全ノードで倍のコストがかかることは避けた(自分側の合法手数は元々詰み判定のために計算済みのものを使い回す)。
- 動作確認: (1)標準将棋でCPU対CPUの応答速度が従来と同じ(~1.6秒/手)でパフォーマンス劣化がないことを確認。(2)将棋vs囲碁で、先手が一手指した直後の後手(AI)の選択が「石を打つ」に変わったことをユニットテストで確認(修正前は評価関数上ほぼ無意味だったため王をふらふら動かすだけだった)。

### 次にやるべきこと(更新版)

- 外部AI接続のレート制限・持ち時間制・ボット専用レーティング
- 両陣営が自由配置できるモードのプライベートマッチでの相互設定フロー(現状は作成者が両陣営とも決める)
- ログイン機能・レーティング反映
- 待機列をRedis等に移行(マルチプロセス/マルチサーバー対応)
- 本番用のPrisma migrationファイルをきちんと作成し直す
- 本番デプロイ構成

## 2026-08-31（続き6）: ログイン/レーティング・AIレーティング分離・マッチング相手タイプ選択・AI強化・棋譜振り返り

大きな機能追加のため、実装内容を分けて記録する。

### ログイン/レーティング

- `User`モデルに`passwordHash`(bcrypt) / `isAi`(AIボット判定) / `gamesPlayed`を追加。ローカル開発DBは`prisma db push --accept-data-loss`で同期(**正式なmigrationファイルはまだ作っていない。本番前に作り直すこと**、と繰り返し記録している通り引き続き未対応)。
- 認証: `apps/server/src/auth.ts`でJWT発行/検証(`jsonwebtoken` + `bcryptjs`)。`POST /api/auth/register`・`POST /api/auth/login`・`GET /api/auth/me`を実装。Authorizationヘッダ(`Bearer <token>`)を使うシンプルな方式で、既存のゲスト対局(未ログインプレイ)は引き続きそのままサポートしている(認証必須にはしていない)。
- フロント: `lib/auth.tsx`にReact ContextベースのAuthProvider(localStorageにtoken+userを保存)。`/login`ページ(ログイン/新規登録の切り替えUI)を新設し、ロビーにログイン状態・レート表示・ログアウトボタンを追加。
- ゲーム作成時(`/api/games/cpu`, `/private`, `/private/join`)にAuthorizationヘッダからuserIdを取り出し、DBの`Game.senteUserId`/`goteUserId`に保存するようにした(**これまでこのフィールドはスキーマにあるのに一度も書き込まれていなかった**)。

### AIレーティングの分離

- AIも`User`テーブルの行として表現し(`isAi: true`)、人間と全く同じレーティング計算パイプラインに乗せることで「別プール」を実現した(`isAi`でフィルタするだけなので実装がシンプル)。
- `apps/server/src/aiProfiles.ts`: 4段階のAI強さプリセット(`ai_easy`/`ai_medium`/`ai_hard`/`ai_expert`、depth 2〜8・思考時間300ms〜6秒)を定義。サーバー起動時に対応するUser行を(`{slug}@bot.local`というメールアドレスをキーに)upsertし、再起動をまたいでも同じアカウント=同じレーティングを使い回せるようにした。
- レーティング更新: `apps/server/src/rating.ts`に標準的なEloレーティング計算(K=32)を実装。対局終了時、両陣営にuserIdが紐づいている場合のみ更新する(ゲスト同士やゲスト vs ログインユーザーの片方だけの対局は対象外)。
- 動作確認: ログイン済みユーザーがAI初級に投了 → 人間側1500→1484、AI初級側1500→1516に正しく更新されることを確認。人間用/AI用のレーティング一覧(`GET /api/users/leaderboard?type=human|ai`)もプールが分かれて表示されることを確認。

### マッチング相手タイプの選択(人間/AI/どちらでも)

- カジュアルマッチ・ランダムルールマッチングの参加時に`opponentPreference: "human" | "ai" | "either"`を指定できるようにした(`apps/server/src/matchQueue.ts`)。
  - `"ai"`: 待機列を経由せず即座にAI対局を作成(実質CPU戦と同じだが、マッチング画面から選べる)。
  - `"human"`: 従来通り、人間の相手だけを待つ。
  - `"either"`: 人間を優先して探し、条件緩和のタイミング(ランダムルールマッチングは既存のルール緩和と同じタイミング、カジュアルマッチは専用タイマー)で見つからなければAIにフォールバックする。**過疎対策(ユーザーが少なくても必ず対局できる)という当初の目的をさらに一歩進める形になった。**
- AI同士の対局(観戦専用): `POST /api/games/ai-vs-ai`で2つのAIプロファイルを指定して対局を作成。人間のトークンは発行せず、`cpuColors`を両方trueにするだけで、既存の「join時にCPU番なら自動着手をトリガーする」仕組みがそのまま両陣営分機能する(**新しい自動対局ループを書く必要はなく、既存のjoinハンドラの副作用だけで実現できた**)。フロントに`/ai-arena`ページを新設し、先手/後手それぞれのAI強さとルールを選んで観戦を開始できるようにした。
- 動作確認: 別プロセスのスクリプトで`opponentPreference:"either"`のカジュアルマッチに参加し、`MATCH_RELAX_TIMEOUT_MS=2000`のもとで2秒後に自動的にAI対局へフォールバックすることを確認。AI同士の対局も、観戦者が誰もソケット接続していない状態から`join`した瞬間に自動再生が始まり、以降は誰も操作しなくても最後まで進行することを確認。

### AIの強化

- 駒の取り合いの手を優先して探索するMVV-LVA的な並び替え(`moveOrderScore`)を追加。捕獲する駒の価値が高いほど、かつ捕獲に使う駒の価値が低いほど優先度を上げる(価値の高い駒で価値の低い駒を取りに行く筋を後回しにする)、成りの手にも小さいボーナスを加えた。同じ時間予算でもより有望な手を先に調べられるようになり、αβ枝刈りの効率が上がる(実効的な探索深さが伸びる)。
- AIプロファイルを4段階(初級〜最強、depth2〜8・思考時間300ms〜6秒)に拡張し、ロビー・CPU戦・AI同士対局のすべてから選べるようにした。
- **正直な制約**: 開放的な定跡データベースや詰将棋ルーチン、置換表(トランスポジションテーブル)は未実装。今回の強化は「評価関数の改善(前回セッションで実施)+手の並び替え改善+探索時間の選択肢拡大」に留まる。本格的な強豪AIを目指すなら、この先は置換表・null move pruning・詰み探索の専用ルーチンあたりが次の壁になる。

### 棋譜振り返り(評価値グラフ・推奨手)

- `apps/server/src/analysis.ts`: 対局の指し手履歴を最初から`GameState`で再生しながら、各局面でAI(`analyzePosition`、既存のnegamax探索を評価値も返すように拡張した`search`関数の薄いラッパー)に評価値と推奨手を出させる`analyzeGameHistory()`を実装。評価値は常に先手視点に正規化(将棋ソフトの評価値グラフと同じ見方)。手数が多い対局で重くなりすぎないよう、1局面あたりdepth2・300msに制限し、解析対象の手数にも上限(既定80手)を設けている。
- サーバー: `GET /api/games/:gameId/analysis`で対局(進行中・終局後問わず)の解析結果を返す。動作確認で12手の対局が約3.4秒で解析できることを確認(1手あたり約280ms、time budgetの設定通り)。
- フロント: `/game/[gameId]/review`ページを新設。インラインSVGで評価値の推移を折れ線グラフ表示(先手有利=上、後手有利=下)し、AIの推奨手と実際の指し手が異なった手を赤い点でハイライト。手ごとの一覧テーブル(実際の手/推奨手/評価値)も表示する。対局画面から「対局を振り返る」リンクで遷移できる。
- 動作確認: ブラウザで実際にCPU戦を投了まで進め、振り返りページでグラフと推奨手一覧が表示されることを確認。

### 次にやるべきこと(更新版)

- 本番用のPrisma migrationファイルをきちんと作成し直す(現状はdb pushで同期しただけ)
- 待機列をRedis等に移行(マルチプロセス/マルチサーバー対応)
- AIの本格強化(置換表・詰み探索・定跡)
- 外部AI接続のレート制限・持ち時間制
- 両陣営が自由配置できるモードのプライベートマッチでの相互設定フロー
- 本番デプロイ構成

## 2026-09-01: .gitignore・Prisma正式マイグレーション・Redis移行・AI Stage1強化

### .gitignore

- リポジトリ直下に`.gitignore`を新設。`node_modules/`・`dist/`・`.next/`・`.env`系・Prismaの生成物・ログ・OS/エディタファイルなどを除外。**このタイミングで気づいたが、いつの間にかリポジトリがgit初期化されていた(前回セッション時点では「gitリポジトリではない」状態だったはず)。** コミットはまだしていない(未指示のため)。

### Prismaの正式なmigrationファイル作成

- これまで`prisma db push --accept-data-loss`でスキーマを直接同期していた分(User認証フィールド・Game.engineGameId/token類)を、正式なmigrationファイルとして書き出した。
- 手順: `prisma migrate diff --from-migrations ... --to-schema-datamodel ... --shadow-database-url ... --script`でSQL差分を生成 → `prisma/migrations/20260831191028_add_auth_and_engine_game_id/migration.sql`として保存 → 実DBは既にdb push済みで内容が一致していたため`prisma migrate resolve --applied`でmigration履歴上「適用済み」とマーク。
- 検証: 新規のクリーンなDB(`shogi_games_migration_test`)に対して`prisma migrate deploy`を実行し、3つのmigration(init → foul_loss追加 → auth/engineGameId追加)が最初から順番に問題なく適用できることを確認。**これで本番/新環境へのデプロイ時に`db push`ではなく正式な`migrate deploy`が使えるようになった。**

### 待機列のRedis移行(マルチプロセス対応)

- ローカルにRedisが無かったため`brew install redis`でインストール。`brew services`がこの環境のlaunchctl経由起動に失敗したため、`redis-server`を直接バックグラウンド起動する運用にしている(**本番では通常のserviceとして起動する想定。ローカル検証用の回避策**、と明記しておく)。
- `apps/server/src/matchQueue.ts`を全面的に書き換え、待機列のエントリ(誰がどのルールで待っているか)をin-memory配列からRedis Hash(`matchqueue:entries`)に移した。複数プロセスが同時にマッチング判定をしても壊れないよう、`redisClient.ts`にSET NX PXベースの簡易分散ロックを実装し、「読み取り→適合判定→書き込み/削除」の一連の操作を排他制御している。
- 緩和/AIフォールバックのタイマーも、プロセスローカルな`setTimeout`から「Redis上に緩和予定時刻を保存 → 各プロセスが1秒ごとにスイープして期限切れのエントリを処理する」方式に変更(`startMatchQueueSweeper`)。これによりどのプロセスでも同じ待機列の状態を見て判断できる。
- socket.ioにも`@socket.io/redis-adapter`を組み込んだ。これがないと、マッチが成立した際に`io.to(socketId)`で通知しようとしても、その`socketId`が別プロセスに接続されている場合は届かない。Redis adapterを入れることで、どのプロセスからでも任意のsocketId宛に届くようになる。
- 動作確認: 2つのソケットクライアントでカジュアルマッチが従来通り成立すること、`MATCH_RELAX_TIMEOUT_MS=2000`でeitherプリファレンスのAIフォールバックが正しく2秒後に発動することを確認。
- **正直な制約(重要)**: 今回Redis化したのは「待機列(誰がどの条件で待っているか)」のみ。**マッチ成立後に作られる対局そのもの(`ServerGame`、盤面・手番などの状態)は依然としてプロセスローカルのメモリ上にしかない。** そのため、もし本当に複数プロセスを水平スケールで動かした場合、Aプロセスの待機列でマッチが成立してもゲーム自体はAプロセスにしか存在せず、Bプロセスに繋がっている側のクライアントは対局に参加できない、という制約が残る。本格的な水平スケール対応には、対局状態自体をRedis/DBで共有するか、同じ対局の両プレイヤーを必ず同一プロセスにスティッキールーティングする仕組みが別途必要(次の課題としてメモ)。単一プロセス運用(現状の実際のデプロイ想定)では今回の変更だけで問題なく動作する。

### AIの本格強化: Stage 1(置換表・静止探索・簡易詰み探索)

- まず`docs/AI_ROADMAP.md`にStage 0〜5の段階的な強化計画を書いた。深層学習・強化学習についても「本格的なNNUE/AlphaZero級は現実的に不可能、まず線形評価の自動チューニング→小型ネットワーク→TD学習、という身の丈にあった順序で」という方針を明記。詰将棋自動生成についても計画セクションを設けた(後述)。
- Stage 1として以下を`apps/server/src/ai/simpleAi.ts`に実装:
  1. **置換表(TT)**: 局面ハッシュ(盤面+持ち駒+手番の文字列)をキーに、探索済みの評価値・最善手をキャッシュ。反復深化の各深さをまたいで同じTTを使い回すことで、浅い深さで見つけた最善手を深い探索の手の並び替えヒントとしても使う。
  2. **静止探索(quiescence search)**: 葉ノードで即座に評価するのではなく、駒の取り合いが続く限り(最大6手)読みを延長してから評価するようにした。地平線効果(次の手で取られる駒を見落とす)を緩和する。
  3. **簡易詰み探索**: 攻め方は「王手になる手」だけを候補にすることで、通常の全幅探索よりずっと高速に数手先(最大6手=3手詰めまで)の強制詰みを発見する。`chooseMove`はまずこの詰み探索を短い時間予算(最大300ms)で試し、詰みが見つかればそれを最優先で指す。
- 動作確認: (1)手動で構築した一手詰め局面で、AIが42msで正しい詰みの手を発見・実際に詰みに至ることを確認。(2)3手詰めを想定した局面でも39msで応答(具体的な詰み手順の妥当性までは今回検証していない)。(3)標準将棋のCPU対局・AI同士対局(ai_hard vs ai_medium)で、TT/静止探索/詰み探索を追加した後も各AIプロファイルの時間予算通りに応答し、レイテンシの劣化がないことを確認。

### 詰将棋の自動生成・出題(計画のみ・未実装)

- `docs/AI_ROADMAP.md`に設計を記載。要点: 完全ランダムではなく、AI自己対局(Stage 3)のログから詰みで終わった対局の終盤を抽出し、今回実装した簡易詰み探索エンジンで「本当に強制詰みか」を検証してから出題候補にする。同じ/類似の詰みパターンが複数回出現したものを「実戦で起きやすい典型パターン」として優先する方針。
- **明示的に今回は実装しない(ユーザー指示通り計画のみ)。** 前提となる自己対局データの蓄積(Stage 3)がまだ無いため、次に着手するとしても自然な順序としてはStage 3の後になる。

### 次にやるべきこと(更新版)

- AI Stage 2(玉の安全度評価・時間配分・定跡)
- AI Stage 3(自己対局によるデータ収集基盤)
- 詰将棋自動生成・出題機能(Stage 1の詰み探索・Stage 3のデータが前提)
- 対局状態自体のマルチプロセス対応(Redis/DB共有 or スティッキールーティング)
- 外部AI接続のレート制限・持ち時間制
- 両陣営が自由配置できるモードのプライベートマッチでの相互設定フロー
- 本番デプロイ構成(Redisの正式なサービス化含む)

## 2026-09-01（続き）: AI Stage 2(玉の安全度・時間配分・簡易定跡)・Stage 3(自己対局データ収集基盤)

### AI Stage 2

- **玉の安全度評価**: 玉(または獅子等のisRoyal駒)の周囲8マスのうち相手に利かされているマスの数を数え、評価に加えた(`kingSafetyScore`)。効率化のため、マスごとに`isSquareAttacked`を8回呼ぶのではなく、相手の全駒の利き筋を1回だけ計算してSetにまとめる方式(`computeAttackMap`)にした。コストを抑えるため、静止探索の一番浅い入り口(qPly=0)でのみ計算し、取り合いの読み最中は毎回再計算しない設計にしている。
- **時間配分**: 反復深化の各深さで最善手が前回と変わった(=局面の評価がまだ安定していない)場合、思考時間を一度だけ30%延長するようにした(`search`関数内)。難しい局面により多くの時間を配分できる。
- **簡易定跡**: `apps/server/src/ai/openingBook.ts`に標準将棋の代表的な初手3種(２六歩・７六歩・５六歩)を用意し、対局の本当に最初の1手(`moveCountSoFar === 0`)だけランダムに選ぶようにした。それ以降は通常通り探索する。棋譜振り返り(`analyzePosition`)は定跡を使わず常に探索結果を返すようにして、振り返り機能の「AIの推奨手」が定跡でごまかされないようにしている。
- 動作確認: 標準将棋CPU対局のレイテンシが従来と同水準(1手あたり約1.6秒、時間延長が発動した場合のみ約2秒)であることを確認。定跡による初手のばらつき(５局中４局が２六歩、1局が７六歩)も確認。

### AI Stage 3: 自己対局データ収集基盤

- `persistMove`/`persistResultIfFinished`を`gameSocket.ts`から`apps/server/src/gamePersistence.ts`に切り出し、通常対局(人間が絡む対局)と自己対局バッチの両方から共通で使えるようにした。
- `apps/server/src/selfPlay.ts`: 観戦者の接続を必要とせず、サーバー内でAI同士の対局を最後まで一気に進める`playSelfPlayGame`/`playSelfPlayBatch`を実装。既存のAI同士対局(`/ai-vs-ai`)は観戦者がsocket接続して初めて指し手が進む設計(joinハンドラの副作用でCPU自動着手ループが回る)だったが、バッチ生成ではその都度接続する必要はないため専用の直接ループを用意した。無限ループ対策として上限手数(600手)も設けている。
- サーバー: `POST /api/games/self-play-batch`で{ルール, 先手AI, 後手AI, 局数}を指定してまとめて対局させる。1リクエストあたり最大20局に制限している(**強いAI同士だと1局に数十秒〜数分かかり、これ以上増やすとHTTPリクエスト自体が長時間ブロックしてしまうため。数百局規模の本格的なバッチ生成にはバックグラウンドジョブキューへの移行が必要、と次の課題として明記**)。
- フロント: `/ai-arena`ページに「自己対局バッチ生成」セクションを追加し、局数を指定してその場で実行→各対局の手数・結果・所要時間を一覧表示できるようにした。
- 動作確認: 3x3盤面で2局(高速)、標準将棋で1局(265手、82.7秒)のバッチ生成を実施。**標準将棋の自己対局で実際に「連続王手の千日手による反則負け」が発生し、これまで単体テストでしか確認できていなかったこのルールが実戦(自己対局)でも正しく機能することを初めて確認できた。** DBに対局・指し手が正しく永続化されること、両AIのレーティング・対局数(`gamesPlayed`)が累積更新されることも確認済み。

### 次にやるべきこと(更新版)

- AI Stage 4(線形評価の自動チューニング → 小型NNUE風ネットワーク → TD学習)。Stage 3で少しずつ蓄積される自己対局データが前提。
- 自己対局バッチのバックグラウンドジョブ化(現状は同期HTTPリクエストで最大20局まで)
- 詰将棋自動生成・出題機能(Stage 1の詰み探索・Stage 3のデータが前提。計画のみ、実装はまだ)
- 対局状態自体のマルチプロセス対応(Redis/DB共有 or スティッキールーティング)
- 外部AI接続のレート制限・持ち時間制
- 両陣営が自由配置できるモードのプライベートマッチでの相互設定フロー
- 本番デプロイ構成(Redisの正式なサービス化含む)

## 2026-09-01（続き2）: 自己対局デーモン(バックグラウンド常時稼働)

ユーザーからの要望: 「AIエージェント(Claude)すら不要で、裏でずっと自己対局等で学習を続けられる環境」「PCへの負荷はあまり大きくない状態で」。

### 実装内容

- `apps/server/src/scripts/selfPlayDaemon.ts`: Claude/エージェントを介さず単独で動き続ける常駐スクリプト。`npm run self-play:daemon -w apps/server`で起動する(`package.json`に追加)。
- **低負荷運用のための工夫**:
  1. `nice -n 15`を付けて起動(npmスクリプト自体に組み込み済み)。OSのスケジューラ上も優先度を下げ、ユーザーの他の作業(このClaude Codeセッション自体を含む)を優先させる。
  2. 1手ごとに小休止(既定400ms、`SELF_PLAY_MOVE_DELAY_MS`で調整可)を入れ、CPUを張り付かせない。
  3. 1局ごとにも休止(既定8秒、`SELF_PLAY_GAME_DELAY_MS`)。
  4. システム全体の`os.loadavg()`(1分平均)をコア数で割った値が閾値(既定0.7、`SELF_PLAY_LOAD_THRESHOLD`)を超えていたら、次の対局を始める前に自発的に一時停止して様子を見る(`waitForLowLoad`)。ユーザーが重い作業をしている時にAI学習が邪魔をしないようにするための安全弁。
- **対戦カードのローテーション**: 標準将棋を中心に、AI強さの組み合わせ(初級vs中級、中級vs上級など)と、5x5・7x7といった盤サイズも少し混ぜたリストを順番に回す(`MATCHUPS`配列)。既存の`playSelfPlayGame`(Stage3で実装済み)をそのまま再利用し、`moveDelayMs`オプションだけ新規に追加した(バッチ生成APIの方は従来通り遅延なしで高速に動く。デーモン専用の低負荷モードとして分離)。
- 動作確認: 3x3盤面での短時間テストで正しく1局完走・DB永続化されることを確認した上で、実際にバックグラウンドプロセスとして起動(`nohup ... &`、ログは`logs/self-play-daemon.log`)。起動直後は標準将棋(初級vs中級)の対局が進行中で、`GameMove`テーブルの件数が実際に増え続けている(=1手ごとに正しくDBへ書き込まれている)ことを確認。`ps`で確認したCPU優先度(NI=20)・プロセス状態も想定通り。
- **今後の課題として明記**: 現状は「データを溜め続ける」段階(AI Stage3)。溜まったデータを使った評価関数の自動チューニング(AI Stage4)はまだ未実装で、次にやるべきこと。またこのデーモンは単一プロセス・単一マシン前提であり、複数のPCで分散して回す仕組みなどは今回のスコープ外。

### 運用メモ

- 停止方法: `pkill -f selfPlayDaemon.ts` (または起動しているターミナルでCtrl+C。SIGINT/SIGTERMを受けたら「今の対局を最後まで終わらせてから」安全に止まるようにしてある)。
- 進捗確認: `tail -f logs/self-play-daemon.log`、またはDBで`SELECT COUNT(*) FROM Game WHERE isSenteCpu=1 AND isGoteCpu=1;`。
- レーティング一覧(`/leaderboard`のAIタブ、または`GET /api/users/leaderboard?type=ai`)でAIの強さの変化(のちの学習後)を追跡できる。現時点ではまだ評価関数自体は学習していないので、レーティングは「対局結果の蓄積」を反映しているだけである点に注意。

### 次にやるべきこと(更新版)

- AI Stage4(線形評価の自動チューニング)着手。デーモンが貯めたデータがある程度溜まってから。
- 詰将棋自動生成・出題機能(計画のみ→着手はStage3データ蓄積後)
- 自己対局バッチAPIのバックグラウンドジョブ化
- 対局状態自体のマルチプロセス対応
- 外部AI接続のレート制限・持ち時間制
- 本番デプロイ構成

## 2026-09-01（続き3）: 持ち時間制・レート制限

自己対局デーモンをバックグラウンドで回しながら、外部AI接続向けの残課題(BOT_API.mdに記載していた「レート制限・持ち時間制は未実装」)に対応した。

### 持ち時間制(切れ負け)

- `GameResult`に`{status:"timeout", winner}`を追加(rule-engine)。`GameState.timeout(player)`メソッドを新設し、resign()と対になる形にした。DBの`GameResultStatus` enumにも`timeout`を追加し、正式なmigrationファイルとして記録(`20260901140038_add_time_control`。前回同様、diffを生成→shadow DBで検証→`migrate resolve --applied`で反映)。
- `Game`テーブルに`timeControlMs`(片側あたりの持ち時間、nullなら無制限)を追加。サーバー再起動時は`GameMove.createdAt`の間隔を再生して残り時間を近似復元する方式にした(消費時間そのものを都度DBに書き込む必要がなく、対局復元の仕組み(Stage3以前から実装済み)にきれいに乗せられた)。
- `ServerGame`に`timeControl`(残り時間・現在の手番の開始時刻)を持たせ、`consumeTurnTimeAndCheckTimeout()`で「着手時に経過時間を消費し、尽きていれば時間切れを返す」処理を実装。着手イベント自体でのチェックに加えて、`maybeScheduleTimeout()`で「残り時間ぴったりのタイミングで発火するsetTimeout」も仕込み、誰も着手しないまま持ち時間が切れた場合でも自動的に時間切れが成立するようにした(既存のCPU自動着手と同じ「setTimeoutで予約→状態が変わっていないか確認してから実行」というパターンを踏襲)。
- プライベートマッチは、作成時点ではなく2人目が参加した時点で時計をリセットするようにした(**当初、作成直後から時計が進んでしまうバグに気づいて修正**。相手を待っている間の時間は消費されない)。
- フロント: ロビーに持ち時間セレクタ(無制限/3分/5分/10分/30分)を追加、対局画面に両陣営の残り時間を表示するクロックUI(残り30秒未満で赤くなる)を実装。サーバーからのスナップショットをクライアント側で1秒ごとにローカル減算して表示する方式(サーバーは着手のたびにしか状態を送らないため)。
- 動作確認: `consumeTurnTimeAndCheckTimeout`を2秒の持ち時間で直接動かし、2秒後に正しく時間切れ(`{status:"timeout", winner:"gote"}`)になることを確認。REST+socket経由でも`timeControl`が正しく状態に含まれて配信されることを確認。

### レート制限

- REST API: `express-rate-limit`を導入。認証系(`/api/auth/*`)は15分間20回まで、対局作成系(`/api/games/cpu`・`/private`・`/private/join`・`/ai-vs-ai`・`/self-play-batch`)は1分間20回までに制限。
- Socket.ioイベント: `express-rate-limit`はHTTPミドルウェア用なのでそのままは使えず、`apps/server/src/rateLimit.ts`に簡易な自前のスライディングウィンドウ制限(`checkSocketRateLimit`)を実装。`move`イベントは1分120回まで(人間には十分すぎる余裕を持たせつつ暴走ボット対策)、`queue:join-*`は1分10回まで。ソケット切断時に記録をクリアしてメモリリークを防いでいる。
- **正直な制約**: Socket側のレート制限はプロセスローカルのメモリ管理。マルチプロセス化(前回セッションのRedis移行は待機列のみ)する場合はここもRedis等に移す必要がある、と記録しておく。

### 自己対局デーモンの状況

- 前回起動したデーモンは今回の一連のサーバー再起動・ビルド作業と完全に独立したプロセスとして動き続けており、影響を受けていないことを確認。現時点で自己対局3局が完走(千日手系の反則負け2局・詰みでの決着1局)、DB上の自己対局数は12局分(累積)に到達している。

### 次にやるべきこと(更新版)

- AI Stage4(線形評価の自動チューニング)。デーモンのデータがもう少し溜まってから。
- 詰将棋自動生成・出題機能(計画のみ)
- 対局状態自体・レート制限のマルチプロセス対応(現状は待機列のみRedis化済み)
- 本番デプロイ構成(Dockerfile・docker-compose・環境変数テンプレート等)

## 2026-09-01（続き4）: AI Stage4着手(線形評価の自動チューニング) — ユーザーからの質問への回答も兼ねて

### 質問への回答: 「対局データはどのくらい溜まっていて、AIの学習に生かされている？」

聞かれた時点(このセッション開始時)での正直な状況:
- **蓄積量**: 自己対局116局、指し手16,287件(自己対局デーモンは今回のセッション作業中も裏で動き続け、ビルド作業などでシステム負荷が上がった際には`waitForLowLoad`が実際に発動して自動的に一時停止していたことも確認できた)。
- **学習への活用**: **していなかった。** 前回のセッションで「AI Stage4(線形評価の自動チューニング)は次の課題」として計画だけ残しており、実装は未着手だった。レーティング(Elo)は対局結果を反映しているが、これは「固定の強さのAI同士がどちらが勝ちやすいか」を測っているだけで、評価関数自体(駒の価値・機動力の重み等)は一切変わっていなかった。

この質問をきっかけに、今回のセッションでAI Stage4の第一歩(計画通りの「線形評価の自動チューニング」)を実装し、実際に今溜まっているデータで1回チューニングを回した。

### 実装内容

- **評価関数の重みを外部化**: `apps/server/src/ai/weights.ts`に`EvalWeights`(駒の価値・機動力の重み・玉の安全度の重み)を切り出し、`simpleAi.ts`内の対局中の探索(negamax/静止探索)は`getWeights()`経由で参照するようにリファクタリング。チューニング済みの重みがあれば`tuned-weights.json`から読み込み、無ければ手作業の初期値を使う。
- **チューニング用の純粋な局面評価関数**: `evaluateStatic(board, hands, turn, ruleSet, weights)`を新設。探索(読み)を一切行わず、与えられた重みで単一局面を評価するだけの関数で、これがチューナーから直接呼べる形になっている。
- **訓練データの抽出**: `apps/server/src/ai/tuning.ts`の`collectTrainingPositions()`が、DBに保存済みの終局した自己対局(**人間が絡む対局は明示的に除外**。手動テストで意図的に変な手を指したものなどが混ざらないようにするため)を`GameState`で最初から再生し、開始直後(定跡的)と終局直前(詰み手順)を除いた中盤〜終盤から1局あたり4局面をサンプリングして「(局面, その局面の手番側視点での最終結果)」のペアを作る。
- **チューニングアルゴリズム**: 本格的な勾配計算はせず、座標降下法(各パラメータを少しだけ動かして、訓練局面に対する予測誤差(評価値をシグモイド関数で勝率に変換したもの vs 実際の結果、の二乗誤差平均)が下がれば採用・下がらなければ元に戻す)というシンプルな方式にした。実装が単純でデータが少なくても暴走しにくい。
- **安全策(下限値)**: 機動力・玉の安全度の重みは、既に将棋vs囲碁バリアントでの石の打ち場所判断など実プレイで有効性を確認済みの項目なので、データ不足でゼロに潰れてしまわないよう元の値の20%を下限に設定した(**最初の試行でmobilityWeightが本当に0まで潰れてしまい、これは危険だと判断して下限を追加した**)。
- 実行: `npm run tune:weights -w apps/server`。既存の`tuned-weights.json`があればそこから継続、無ければ手作業の初期値から開始する。

### 今回の実行結果と、正直な評価

- 自己対局111局から437局面をサンプリングしてチューニングを実行。予測誤差(MSE)は0.20598 → 0.20362とわずかに改善(相対で約1.2%)。
- 駒の価値はかなり大きく動いた(例: 歩1→3.65、金6→3.35、銀5→7.65)が、**MSEの改善幅が小さいわりに値の変動が大きく、これはデータ量に対してパラメータ数が多すぎることによる過学習/ノイズの可能性が高いと考えている。** 現時点でのチューニング結果を「AIが本当に賢くなった」と結論づけるのは時期尚早で、正直なところ「パイプラインが動くことを確認できた」以上の意味は薄いと考えている。
- とはいえ、チューニング後の重みは`tuned-weights.json`として保存し、自己対局デーモンとサーバーの両方が起動時に自動で読み込むようにした(実際に再起動して読み込まれることを確認済み)。これにより、今後デーモンが生成する自己対局のデータ自体も新しい重みでのプレイに基づくものになる(=データとチューニングが回り始める)。
- **今後の方針**: データが数百局→数千局規模に増えるまで、`npm run tune:weights`は定期的に(手動で)再実行して結果を観察するのが良い。改善が安定して見られるようになったら、チューニングの自動実行(デーモン側に組み込む)や、本物の勾配降下・より多いサンプル数、といった改良を検討する。

### 次にやるべきこと(更新版)

- チューニングの継続的な観察(データが増えるごとに`npm run tune:weights`を再実行し、MSEの推移とチューニング後の重みの妥当性を見ていく)
- 過学習対策(正則化・サンプル数を増やす・パラメータ数を減らす等)の検討
- Stage4後半: 小型NNUE風ネットワークへの発展(線形チューニングの効果が頭打ちになってから)
- 詰将棋自動生成・出題機能(計画のみ)
- 本番デプロイ構成

## 2026-09-01（続き5）: AI最終目標の戦略見直し・本番デプロイ構成

### AIの最終目標について(ユーザーとの合意事項)

ユーザーから「最終的に水匠5・やねうら王・ぴよ将棋クラスの強さにしたい」という要望があり、現在のアプローチ(自前TypeScript実装+ローカルMacでの自己対局チューニング)で到達可能か検討・提案し、以下の方針で合意した(詳細は`docs/AI_ROADMAP.md`冒頭に記載):

- **自前エンジンの延長線上ではプロ超えエンジンには届かない**(実装言語・探索技術・学習データ規模のスケールが根本的に違う)と判断し、正直に伝えた。
- **2トラック戦略で合意**: トラックA=自前エンジンは変則ルール専用として現実的な強さ(アマチュア強豪程度)を目指して引き続き強化。トラックB=標準ルール限定で「AI最強」ティアにやねうら王をUSIプロトコル経由の外部エンジンとして統合(別プロセス+標準入出力通信、GPL的にも安全な統合方式)。
- **ライセンス確認の必要性を明記**: やねうら王本体(GPLv3)は別プロセス統合なら配布義務は生じないという解釈が一般的だが、評価関数ファイル(水匠5等)は作者ごとに商用利用条件が異なるため、商用サービスで使う前に個別確認が必須、という点を強調して記録した。
- トラックBの実装(USIプロセス管理・SFEN変換・AIプロファイル統合)は具体的な設計を`docs/AI_ROADMAP.md`に書いたが、**今回のセッションでは方針決定のみで実装はしていない**(次のステップ)。

### 本番デプロイ構成

Highstoの既存サービス(rule.highsto.net等)と同じ**Nginx + PM2 + MySQL + Redis**構成に合わせて整備した(`~/.claude/knowledge/services.md`・`development.md`の運用パターンを踏襲)。

- `ecosystem.config.js`: PM2でserver(4000番)・web(3100番)・自己対局デーモンの3プロセスを管理。対局状態がプロセスローカルなため`instances: 1`固定にしている旨をコメントで明記。
- `deploy/nginx.conf.example`: フロントとAPI/Socket.ioをパスで振り分けるNginx設定サンプル。Socket.ioに必須のWebSocketアップグレードヘッダ・タイムアウト延長を含む。
- `docs/DEPLOYMENT.md`: 初回セットアップ手順・通常更新デプロイ手順(Highstoの他プロジェクトと同じ`git pull → npm ci → build → pm2 reload`の流れ)・環境変数チェックリスト・既知の制約(マルチプロセス非対応等)をまとめた。
- **重要な抜け漏れに気づいて修正**: `apps/server`にこれまで`.env`を読み込む仕組みが無く(開発中はシェルの環境変数で都度渡していた)、本番でPM2から`.env`経由で`JWT_SECRET`等を設定する運用ができない状態だった。`dotenv`を導入し、`index.ts`と`selfPlayDaemon.ts`の先頭で読み込むように修正した。
- `.env.example`(apps/server, apps/web, packages/db)を追加。`.gitignore`も`.env*`全般を除外するよう修正(以前は`.env.local`等の限定パターンのみで、本番用の`.env.production`等が漏れる状態だった)。
- **Docker構成は補助的に用意**(`Dockerfile`×2、`docker-compose.yml`)。Highstoの現在の運用はPM2直接運用でDocker非使用だが、VPSが「障害多発により移行検討中」という状況を踏まえ、将来のコンテナベース移行の選択肢として最小限用意した。メイン運用はあくまでPM2の方であることを明記。
- 動作確認: ローカルにPM2をインストールし、`ecosystem.config.js`で実際に3プロセスを起動 → ヘルスチェック・ロビー表示・CPU戦の開始/棋譜ダウンロード/振り返り機能までブラウザで一通り確認。**確認後にPM2プロセスは停止し、元々動かしていた自己対局デーモン(standalone)に戻した**(誤って一時的にデーモンが重複起動したため1つに戻す作業も実施)。

### トラックB: やねうら王USI統合(2026-09-01実装)

docs/AI_ROADMAP.mdの計画に沿って実装した。要点:

- `apps/server/src/ai/usiEngine.ts`(USIプロセスの薄いラッパー)、`ai/sfen.ts`(標準ルール専用のSFEN相互変換)、`ai/chooseMoveForProfile.ts`(`engineType`で内蔵AI/外部USIエンジンを振り分ける非同期ディスパッチャ)を新設。`selfPlay.ts`・`socket/gameSocket.ts`のCPU着手箇所をこの非同期版に置き換えた。
- `AiProfile`に`engineType?: "internal" | "usi"`・`usiEnginePath?`・`usiOptions?`を追加。`aiProfiles.ts`で環境変数`YANEURAOU_ENGINE_PATH`が設定されている場合のみ`ai_yaneuraou`プロファイルを追加する(未設定なら従来通り内蔵AIのみ)。
- **実機確認まで実施**: やねうら王本体(GPLv3, github.com/yaneurao/YaneuraOu)を`vendor/YaneuraOu`にソースからclone・ローカルビルドした(`YANEURAOU_ENGINE_MATERIAL`エディション=駒得のみの評価関数、Apple Silicon向け`APPLEM1`ターゲット、`vendor/bin/yaneuraou-material`)。NNUE評価ファイルを使わない構成にしたことで、ライセンス未確認のまま評価関数を配布・使用するリスクを避けつつ、SFEN変換・USIプロセス通信・指し手パースの一連の経路を実データで検証できた(標準初期局面から実際に指し手が返ってくることを確認)。
- SFEN変換は初期局面が公式のSFEN初期値と完全一致することをテストで確認済み(`lnsgkgsnl/1r5b1/ppppppppp/9/9/9/PPPPPPPPP/1B5R1/LNSGKGSNL b - 1`)。
- `vendor/`は`.gitignore`に追加(ソースツリー・ビルド成果物が大きく、各自の環境でビルドし直す前提のため)。

**まだやっていないこと(重要)**:
- 今動いているのは駒得のみの評価関数であり、水匠5/やねうら王NNUE版のような強さは出ていない。本当に強くするにはNNUE評価関数ファイルの入手が要るが、**そのライセンス確認は未着手**。商用(Highsto実サービス)投入前に必ず個別確認すること。
- 本番(ConoHa VPS、おそらくx86_64 Linux)向けのビルドはまだ行っていない(今回はmacOS/Apple Silicon向けのみ)。
- ~~Webソケット経由でのCPU対戦としての実地テスト~~ → 下記の通り追加セッションで実施し、実害のあるバグを2件発見・修正した。

### 詰将棋自動生成・出題機能(2026-09-01実装)

計画のみだった機能を実装した。詳細はdocs/AI_ROADMAP.mdの該当セクション参照。要点:

- `Puzzle`モデル追加(出題局面・検証済み解答手順・詰み手数・出典対局ID・出現回数)。
- `apps/server/src/puzzles.ts`: 自己対局データ(checkmateで終局したAI同士の対局)から`findForcedMate`で検証し直した詰み手順を抽出。玉周りの駒配置による重複検出、一手ごとの合法性検証・別解許容の詰み継続判定を実装。
- API(`/api/puzzles`他)とWeb UI(`/puzzles`, `/puzzles/[puzzleId]`)を実装し、実際にブラウザで1手詰め問題を解いて正解表示まで確認した。
- CLI(`npm run puzzles:generate -w apps/server`)で自己対局99局から8問生成できることを確認(重複除いた新規パターンのみ登録)。

### 次にやるべきこと(更新版)

- トラックBの残課題: NNUE評価関数のライセンス確認、Linux向けビルド
- AI Stage4の継続観察(データが増えるごとに`npm run tune:weights`)
- 詰将棋機能の残課題: 生成頻度の運用(今は手動`npm run puzzles:generate`。定期実行の仕組み化は未着手)、成銀・成香等の細かい成り判定UXの改善
- 対局状態自体・レート制限のマルチプロセス対応
- 実際のConoHa VPSへの初回デプロイ(今回はローカルでの動作確認のみ。本番ドメイン・SSL証明書等は未設定)

### 2026-09-01: Webソケット実地テスト(トラックB)で見つけた重大バグ2件

3(トラックBのブラウザ実地テスト)を実施したところ、実際に使ってみて初めて分かる不具合が2つ見つかり、その場で修正した。

1. **SFEN変換の筋(すじ)変換式が逆だった**: `apps/server/src/ai/sfen.ts`の`fileFromCol`/`colFromFile`が`9-col`になっていたが正しくは`col+1`。盤面全体を変換する`boardToSfen`は列挙順で偶然正しい結果になっていた(だからさっきの単体テストでは初期局面が正解と一致していた)一方、1マスだけを変換する`squareToUsi`/`squareFromUsi`(指し手のデコードに使う)は式が逆になっており、**やねうら王が返した指し手を誤った位置にデコードしてしまい、盤面が壊れ、最終的にサーバー全体がクラッシュする**という実害のあるバグだった。ブラウザでの実地テストをせず`chooseMoveForProfile`の単体呼び出しだけで確認していたら見逃していた可能性が高い。
2. **CPU着手の非同期処理で例外を握りつぶしていなかった**: 上記のバグで壊れた盤面に対してさらにやねうら王へ指し手を要求した結果、`chooseMoveForProfile`が例外を投げ、それが`socket/gameSocket.ts`の`setTimeout`コールバック内でunhandled rejectionとなり、**Node.jsプロセス全体がクラッシュ**した。外部エンジン連携を入れたことで「CPU側が指し手を返せない」ケースが現実的に起こりうるようになったにもかかわらず、その経路に例外処理が無かった。`try/catch`で囲み、失敗をログに残すだけでプロセスは継続するように修正。
3. ついでに、AI-vs-AI観戦(`/game/[gameId]`)がそもそも**一度も正常に動いていなかった**ことも発覚: `ai-arena`から対局を開始すると`sessionStorage`にトークンを保存しないため、観戦ページ側の「セッションが無ければエラー画面」という分岐に必ず引っかかっていた。サーバー側は元々観戦(spectator)を正しくサポートしていたので、単にこの早期returnを削除するだけで直った。

いずれも「実装しただけ」では気づけず、実際にブラウザで最後まで動かして初めて見つかった問題。今後も新しい経路を追加したときは、可能な範囲で実地確認まで行うことを徹底したい。

### 2026-09-02: ユーザー報告バグ2件(盤面表示・成り駒)の修正、過去データのリセット、UI改善

ユーザーから「飛車と角の位置が逆」「成駒が成駒の動きをできない」という報告があり、両方とも実際にバグだった。

1. **盤面の筋(列)の表示順が逆**: `Board.tsx`等で列を反転せず左から右にそのまま描画していたため、先手視点で角と飛車の位置が実際の将棋の盤面図と逆に見えていた。データは正しく表示のみの問題。同じ描画パターンを使っていた3箇所(対局画面・詰将棋・自由配置)を修正。
2. **成駒が成る前の動きしかできない重大バグ**: `applyMoveToBoard()`が成った駒の`promoted`フラグだけを立てて`kind`(歩→と金等)を変えていなかったため、合法手生成が常に成る前の動き方で計算されていた。`kind`を成り後の駒種に切り替えるよう修正。AIの評価関数の駒価値テーブルやUIの駒表示ラベルは元々「成り駒のkindが変わる」前提で作られていたため、この修正で副次的にそれらも正しく機能するようになった。`baseKindOf()`をpackages/rule-engineの共通関数として切り出し、重複実装を統合。SFEN変換への影響も修正。

**過去データのリセット**: 成り駒バグは以前からずっと存在していたため、これまでの自己対局データ(140局超)は成りが絡む対局結果が実際のルールと異なっている可能性があった。ユーザーの指示で過去のGame/GameMove/Puzzle/チューニング済み重み(tuned-weights.json)を全て削除し、User(AI・テストアカウント)のrating/gamesPlayedを初期値にリセットした。自己対局デーモンも修正後のコードで再起動し、クリーンなデータの蓄積を再開した。

**副次的に見つかった別の重大バグ**: データリセット作業中、サーバーがDBに存在しない対局IDへ指し手を保存しようとして未処理例外でクラッシュした。`socket/gameSocket.ts`の`move`/`resign`ハンドラに例外処理が無く、DB不整合等の想定外エラーで**プロセス全体が落ち、進行中の全対局が巻き添えになる**状態だった。try/catchで囲み修正(CPU着手タイマー側は本セッションの前半で既に同様の修正済みだったが、人間の着手/投了ハンドラには手を入れていなかった)。

**UI改善(ユーザー要望)**:
- 持ち駒の並び順を「飛角金銀桂香歩」に統一(`Board.tsx`の`HandPanel`)
- 駒の見た目を五角形の駒形+影付きにする`PieceGlyph`コンポーネントを新設し、対局画面・自由配置・詰将棋の3箇所で共通利用。成り駒は`pieceLabel`がkind(バグ修正により正しく成り後のkindになった)から正しい文字(と・成銀・龍等)を返すのでそのまま表示される
- 詰将棋の出題で、攻め方が後手の場合も画面下側に来るよう盤面を180度回転する処理を追加(既存の対局画面の後手視点ロジックと同じ考え方)

### 2026-09-02: socket/HTTPハンドラの例外処理を全面監査

前段のDB不整合クラッシュを機に、同種の問題が他に残っていないか全ハンドラを洗い出した。

- `socket/gameSocket.ts`: `join`ハンドラ(同期例外はSocket.ioが拾わないため未対応だとプロセスが落ちる)、`maybeScheduleTimeout`の`setTimeout`コールバックにtry/catchを追加
- `socket/matchSocket.ts`: `queue:join-casual`・`queue:join-random`・`queue:leave`・`disconnect`の全ハンドラにtry/catchを追加(Redis経由のキュー処理でエラーが起きるとプロセス全体が落ちる状態だった)
- **Express 4はasyncなルートハンドラの例外を自動で拾わない**(Express 5との違い)ため、`asyncHandler`という共通ラッパー(`apps/server/src/asyncHandler.ts`)を新設し、DB操作を含む全ルート(games/auth/users/puzzles)に適用。合わせてExpress側にもグローバルエラーハンドリングミドルウェアを追加し、拾われた例外はJSON形式の500エラーとして返すようにした
- 最終防衛ラインとして`process.on("unhandledRejection"/"uncaughtException")`をAPIサーバー・自己対局デーモンの両方に追加(ログのみ、プロセスは継続)。今後未知の経路で同種の問題が起きても、進行中の全対局が巻き添えでクラッシュすることは無くなったはず
- 動作確認: 存在しない対局ID・詰将棋IDへのアクセス、不正なプロファイルslug指定等でも500ではなく想定通りのレスポンスが返り、サーバーが継続稼働することを確認

### 2026-09-04: 過去の対局履歴を見れるように、AIの強さ戦略の相談、やねうら王評価関数の強化

- 過去の自己対局データを一覧・棋譜ダウンロードできる`/history`ページを追加(サーバー再起動でメモリから消えた対局もDBから復元できるようにした)。詳細はdocs/AI_ROADMAP.md参照不要、上の見出しに記載済み。
- ユーザーから「自己対局データを増やせばアマ初段に届くか」という質問があり、MSEの推移(データが増えるほど悪化している)を根拠に、正直に「データ量では届かない、評価関数・探索エンジンの構造的な限界」と回答。続けて「このPCで回せる範囲でそこそこ強いものを作るアーキテクチャは？」という相談を受け、**探索は自作せずやねうら王(GPL)をそのまま使い、評価関数だけ強化する3段階案**(A: 手作り評価関数強化 / B: 自前学習NNUE / C: 継続的強化学習)を提案し合意。
- **トラックB Stage A実装**: やねうら王のソースに評価関数強化の連載実装(MATERIAL_LEVEL 1〜9、駒の利き・玉の囲い評価などを段階的に追加)が既に同梱されていたため、Makefileの定数を1→9に変更してリビルドするだけで実装できた(コード変更ゼロ)。詳細はdocs/AI_ROADMAP.mdの該当セクション参照。
- **強さの検証**: レベル1とレベル9を直接対局させるベンチマークスクリプト(`apps/server/src/scripts/benchmarkEngines.ts`)を新設し、思考時間を変えて検証。**0.5秒思考ではレベル9が2敗、1秒だとほぼ互角(11勝9敗)、3秒だと6勝2敗(75%)とレベル9が明確に優勢**という結果になった。評価関数強化は計算コストとのトレードオフで、短い持ち時間だと逆に弱くなりうることが分かったため、`.env`の`YANEURAOU_BYOYOMI_MS`を1500→3000に変更した。3秒×16局のテストは、CPU負荷とファン音が気になったためユーザーの判断で8局時点で打ち切った(傾向は十分見えていたため)。

### トラックB Stage B着手: 自前学習NNUEの基盤実装(2026-09-04)

「Bに進んで」との指示を受け、自前学習NNUE評価関数の土台部分を作り込んだ。やねうら王のNNUEファイル形式は
ドキュメント化されていないため、ソースコードを直接読んで解析する地道な作業になった(BonaPiece特徴量
エンコーディング、バイナリのヘッダ・各層の量子化形式・重みの並び順など)。

- `ai-training/`にPython環境を新設(PyTorch、Apple SiliconのGPU=MPSバックエンドが使えることを確認)。
- `nnue_format.py`(特徴量エンコード)・`export_nnue.py`(バイナリ書き出し)を実装。
- **実機での往復検証に成功**: ゼロ初期化のダミー重みを書き出し、別途ビルドしたNNUE版やねうら王
  (`vendor/bin-nnue/`)が実際にクラッシュせず読み込み、指し手を返すことを確認した。ファイルサイズ・
  レイヤー構造が完全一致していないと失敗するテストなので、フォーマット理解が正しいことの実証になっている。
- 途中、FeatureTransformer層の重みが通常のニューラルネット層とは転置されたレイアウト(feature-major)に
  なっているという、ソースを読まないと分からない罠を発見し対応した。
- 次のステップは学習データパイプラインと実際の学習ループ(まだ重みは意味のある値になっていない)。
  詳細はdocs/AI_ROADMAP.mdのトラックB Stage Bセクション参照。
