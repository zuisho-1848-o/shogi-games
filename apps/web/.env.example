# Next.jsから見たサーバー(apps/server)のURL。ブラウザから直接fetch/socket.io接続するため
# NEXT_PUBLIC_ プレフィックスが必要(ビルド時に埋め込まれる)。
# 本番では実際のAPIドメイン(例: https://shogi-api.highsto.net)に変更すること。
NEXT_PUBLIC_SERVER_URL=http://localhost:4000
